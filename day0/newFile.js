const { operator, num1, num2 } = require("./ex7");

switch (operator) {
    case "+": ;
        console.log(num1 + num2);
        break;
    case "-": ;
        console.log(num1 - num2);
        break;
        if (num2 == 0)
            console.log("division not allowed");
        elseconsole.log(numa);
}
